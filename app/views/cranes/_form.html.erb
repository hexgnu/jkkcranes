<%= form_for @crane, :html => {:multipart => true} do |f| %>
  <% if @crane.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@crane.errors.count, "error") %> prohibited this crane from being saved:</h2>
    <ul>
    <% @crane.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>	
	<%= f.file_field :pdf %>
  <div class="field">
    <%= f.label :owner %><br />
    <%= f.text_field :owner %>
  </div>
  <div class="field">
    <%= f.label :city %><br />
    <%= f.text_field :city %>
  </div>
  <div class="field">
    <%= f.label :comments %><br />
    <%= f.text_area :comments %>
  </div>
	<%= f.text_field :date, :id => :datepicker %>
	
	<%= f.submit %>	
<% end %>
